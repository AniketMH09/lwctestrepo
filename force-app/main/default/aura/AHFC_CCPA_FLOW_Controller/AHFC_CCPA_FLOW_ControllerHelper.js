({
    /*init : function (component) {
        // Find the component whose aura:id is "AHFC_Payment_Due_Date_Change_Request_Flow"
        var flow = component.find("CCPA_Request_Community_Flow");
        // In that component, start your flow. Reference the flow's Unique Name.
        flow.startFlow("myFlow");
    },*/

    /* findGetParameter : function(var1, var2) {
         // do something with var1 and var2 here
    }, */
})